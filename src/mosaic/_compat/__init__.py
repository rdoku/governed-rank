"""
Compatibility modules that depend on the external patterna package.

These modules (loaders, state) are used when MOSAIC is deployed within
the full PatternaAi server stack. They are not required for standalone
use of the MOSAIC algorithm.
"""
